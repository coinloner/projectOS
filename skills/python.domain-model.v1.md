# Python Domain Model

适用于领域层和纯业务规则实现。

## 做法

- 使用无副作用的实体、值对象和领域服务表达业务规则。
- 领域层不得导入 FastAPI、数据库 ORM、网络客户端或环境变量读取器。
- 用显式类型和不变量校验拒绝非法状态；错误类型要能被应用层转换。
- 保持领域对象可在无数据库、无 HTTP 的情况下单元测试。

## 自检

- 是否能独立导入并运行？
- 是否把持久化细节留在 infrastructure？
- 每条关键规则是否有正例和反例测试？
