# Python HTTP Service

适用于 FastAPI 或标准库 HTTP 后端的接口层与应用层实现。

## 做法

- 路由只负责解析输入、调用应用服务和映射响应，不承载业务规则或 SQL。
- 应用服务负责用例编排、事务边界和错误到 API 语义的转换。
- 响应结构和错误格式保持稳定；健康检查不能依赖业务数据。
- 异步接口不得在事件循环中执行阻塞文件或数据库操作。

## 自检

- `/health` 是否可独立启动并返回就绪状态？
- 无效输入、资源不存在和冲突是否有明确状态码？
- 接口测试是否只通过公开 HTTP 契约验证行为？
