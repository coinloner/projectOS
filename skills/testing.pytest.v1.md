# Python Testing

适用于后端单元、应用服务、接口和集成测试。

## 做法

- 领域规则优先使用快速单元测试，接口行为使用 HTTP 测试，数据库行为使用隔离测试库。
- 每个测试只验证一个可观察行为，失败信息要指出输入和预期。
- 测试不得依赖运行顺序、开发机上的真实服务或未声明的网络资源。
- 对错误路径、重复请求和边界输入至少各覆盖一条用例。

## 自检

- 测试命令是否能由 runtime manifest 直接执行？
- 是否存在只检查文件存在而没有验证行为的伪测试？
- 失败时是否保留 stdout、stderr 和运行环境证据？
