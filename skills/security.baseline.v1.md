# Security Baseline

适用于所有生成项目的基础安全检查。

## 做法

- 不把密钥、令牌、密码或内部路径写入代码和提交产物。
- 所有外部输入都在边界处校验；数据库查询使用参数化接口。
- 文件路径、命令参数和 URL 不能直接信任用户输入。
- 默认拒绝未声明能力，日志避免记录完整凭证和敏感业务数据。

## 自检

- 是否存在硬编码凭证或调试后门？
- 错误响应是否泄漏堆栈、SQL 或环境变量？
- 新增依赖是否有明确来源和版本约束？
