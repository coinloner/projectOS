# Native Web Frontend

适用于无构建工具的 HTML、CSS 和 JavaScript 前端。

## 做法

- 按页面状态组织代码：加载、空态、成功、错误和提交中状态必须可区分。
- API 调用集中在 client 模块，视图层不拼接 URL 或重复处理错误。
- 表单提交要防止重复请求，并在失败后保留用户输入。
- 使用语义 HTML、键盘可操作控件和响应式布局。

## 自检

- 首屏能否在后端启动后直接打开？
- 网络失败时是否有可见反馈？
- 主要流程是否能用 Playwright 或浏览器自动化复现？
